# Secrets / credentials — NEVER commit these
.env
.env.*
kaggle.json
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Data — too large for git, downloaded separately (see data/README.md)
data/*.csv

# NOTE: the final trained model (models/ecg_cnn.pt, ~182 KB) and the result
# artifacts (results/confusion_matrix.png, classification_report.txt) ARE
# committed on purpose so the repo is clone-and-run and the README renders the
# confusion matrix. They're small. Re-generate any time with:
#   python -m src.train && python -m src.evaluate

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
